/** Host loader entry for the skills-manager tab browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the skills manager tab. */
export function apply(): void {}
